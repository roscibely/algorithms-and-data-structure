## Exemplo de Tipo abstrato de dados 

## Tipo Circulo 

Para compilar o exemplo: 


```console
    gcc -c ponto.c
    gcc -c circulo.c
    gcc -c main.c 
    gcc -o programa.exe ponto.o circulo.o main.o 
    programa
```
